apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: service-discover-parser-lib
  title: Service Discover Parser Library
  description: |
    A library contains a ackage parser which provides an abstraction layer over the parser engine.
    This package allows and integrates possible plugin and additional components over Kong,
    that is the real engine that parses the input arguments.
  annotations:
    backstage.io/techdocs-ref: dir:.
  tags:
    - service-discover
    - cli
    - lib
    - library
    - parser
    - kong
    - consul
    - go
    - backend
spec:
  type: library
  lifecycle: production
  owner: new-bacon-monsters